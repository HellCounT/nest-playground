services:
  postgres:
    image: postgres:latest
    container_name: nest-playground-postgres
    environment:
      POSTGRES_DB: nest_playground
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
